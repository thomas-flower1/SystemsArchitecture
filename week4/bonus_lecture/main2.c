#include <stdio.h>



int main() {

    // modular programming in c
    // at the moment we have been only doing programs in a single file

    // any lines with the # is for the preprocessor, we have done include and define (for creating macros)

    #ifdef DEBUG

        printf("Hello world\n");

    #endif

    // able to define these macros on the command line when we compile

    // modularity, using header files
    





    return 0;
}